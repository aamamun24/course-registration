import PropTypes from 'prop-types';

const Carts = (props) => {
    return (
        <div className='w-1/4'>
            <h2>this is carts</h2>
        </div>
    );
};

Carts.propTypes = {
    
};

export default Carts;