import PropTypes from 'prop-types';
import Card from '../Card/Card';

const Cards = (props) => {
    return (
        <div className='w-3/4'>
            <Card></Card>
        </div>
    );
};

Cards.propTypes = {
    
};

export default Cards;